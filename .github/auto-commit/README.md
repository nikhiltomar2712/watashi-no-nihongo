Local auto-commit is configured via systemd (Linux) and runs change-based commits.

This repo automation does NOT generate spammy commit volume; it commits only when there are real working tree changes.

